name: FTS Integration Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  NIXPKGS_ALLOW_UNFREE: 1

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: cachix/install-nix-action@v27
        with:
          nix_path: nixpkgs=channel:nixos-unstable
          extra_nix_config: |
            experimental-features = nix-command flakes

      - uses: cachix/cachix-action@v15
        with:
          name: fasttrackstudio
          # authToken: '${{ secrets.CACHIX_AUTH_TOKEN }}'
        continue-on-error: true

      - name: Build FTS test environment (CI preset)
        run: nix build .#fts-test-ci --impure

      - name: Smoke test — REAPER starts headless
        run: nix run .#fts-test-ci --impure -- ./ci/test.sh

      # Uncomment when daw crate tests are Linux-ready:
      # - name: Clone daw repo
      #   uses: actions/checkout@v4
      #   with:
      #     repository: FastTrackStudios/daw
      #     path: daw
      #
      # - name: Run integration tests
      #   run: |
      #     nix run .#fts-test-ci --impure -- bash -c "
      #       cd daw
      #       cargo test -p daw-reaper --test reaper_connection -- --ignored --nocapture
      #     "
